
@using TimeSheet.Models
@model  CandidateTimeSheetsModel
@{
    ViewBag.Title = "Login";
    //Layout = "~/Views/Account/Login.cshtml";
}
<link href="//netdna.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
<script src="//netdna.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
<script src="//code.jquery.com/jquery-1.11.1.min.js"></script>
<!------ Include the above in your HEAD tag ---------->
<script src="//ajax.googleapis.com/ajax/libs/jqueryui/1.10.4/jquery-ui.min.js"></script>
<br>
<table class="table table-responsive" id="tab">
        <div class="form-group">
            <div class="row">
                <div class="col-md-4">
                    <label>DATE :</label>
                    <input type="date" class="form-control">
                </div>
                <div class="col-md-4">
                        <label>PROJECT :</label>
                        <input type="text" class="form-control">
                    </div>
                <div class="col-md-4">
                    <label>CANDIDATE :</label>
                    <select id="country" name="country" class="form-control">
                        <option value="Peter">Peter</option>
                        <option value="Jack">Jack</option>
                        <option value="Tom">Tom</option>
                        <option value="Leah">Leah</option>
                    </select>
                </div>
                <div class="col-md-4">
                    <label>STATE:</label>
                    <select id="country" name="country" class="form-control">
                        <option value="NSW">NSW</option>
                        <option value="ACT">ACT</option>
                        <option value="QLD">QLD</option>
                        <option value="VIC">VIC</option>
                        <option value="SA">SA</option>
                        <option value="WA">WA</option>
                        <option value="NT">NT</option>
                        <option value="TAS">TAS</option>
                    </select>
                </div>
            </div>

        </div>

    </table>

@*<div class="container">
    <div class="row clearfix">*@
        <div class="table-responsive">
            <table class="table table-bordered table-hover table-sortable" id="tab_logic">
                <thead style="font-weight:400; background-color:#FFA500;color:white;">
                    <tr>

                        <th class="text-center" >
                            Date
                        </th>
                        <th class="text-center" >
                            Warehouse
                        </th>
                        <th class="text-center">
                            Project
                        </th>
                        <th class="text-center">
                            JobNo
                        </th>
                        <th class="text-center">
                            OpportunityNo
                        </th>
                        <th class="text-center">
                            Activity
                        </th>
                        <th class="text-center">
                            Resources
                        </th>
                        <th class="text-center">
                            StartTime
                        </th>
                        <th class="text-center">
                            EndTime
                        </th>
                        <th class="text-center">
                            OLATarget
                        </th>
                        <th class="text-center">
                            ActualQuantity
                        </th>
                        <th class="text-center" style="border-top: 1px solid #ffffff; border-right: 1px solid #ffffff;">
                        </th>
                    </tr>
                </thead>
                <tbody>
                    <tr id='addr0' data-id="0" class="hidden">

                        <td data-name="name">
                            <input type="date" name='name0' placeholder='Date' class="form-control" />
                        </td>
                        <td data-name="name">
                            <select name="sel0" class="form-control">
                                <option value""></option>
                                <option value="NSW">NSW</option>
                                <option value="ACT">ACT</option>
                                <option value="QLD">QLD</option>
                                <option value="VIC">VIC</option>
                                <option value="SA">SA</option>
                                <option value="WA">WA</option>
                                <option value="NT">NT</option>
                                <option value="TAS">TAS</option>
                            </select>
                        </td>
                        <td data-name="name">

                            @Html.DropDownListFor(model => model.Id, Model.Customerlist, "Select a Project", new { @class = "form-control project" })

                            @*<input type="text" name='name0' placeholder='Project' class="form-control" />*@
                        </td>

                        <td data-name="mail">
                            <input type="text" name='mail0' placeholder='Job Number' class="form-control" />
                        </td>
                        <td data-name="mail">
                            @Html.DropDownListFor(model => model.Id, Model.OpportunityNumberList, "Opp No", new { @class = "form-control project" })

                            @*<input type="text" name='mail0' placeholder='Opp No' class="form-control" />*@
                        </td>
                        <td data-name="desc">
                            @Html.DropDownListFor(model => model.Id, Model.ActivityList, "Activity", new { @class = "form-control project" })

                            @*<input name="desc0" placeholder="Activity" class="form-control">*@
                        </td>
                        <td data-name="mail">
                            <input type="text" name='mail0' placeholder='' class="form-control" />
                        </td>
                        <td data-name="mail">
                            <input type="time" name='mail0' placeholder='Start Time' class="form-control" />
                        </td>
                        <td data-name="mail">
                            <input type="time" name='mail0' placeholder='End Time' class="form-control" />
                        </td>
                        <td data-name="mail">
                            <input type="number" name='mail0' placeholder='OLA' class="form-control" />
                        </td>
                        <td data-name="mail">
                            <input type="number" name='mail0' placeholder='Qty' class="form-control" />
                        </td>

                        <td data-name="del">
                            <button nam"del0" class='btn btn-danger glyphicon glyphicon-remove row-remove'></button>
                        </td>
                     
                    </tr>
                </tbody>
            </table>
        </div>
    @*</div>*@
    <a id="add_row" class="btn btn-default pull-right" style="background-color:lightgray">Add Row</a> <a id="Submit" class="btn btn-default pull-left" style="background-color:lightgray">Submit</a>
@*</div>*@


<style>
    .entry:not(:first-of-type) {
        margin-top: 10px;
    }

    .glyphicon {
        font-size: 12px;
    }

    .table th {
        width: 5000px;
    }
    .table-sortable tbody tr {
        cursor: move;
    }


</style>
<script>
    $(document).ready(function () {
        $("#add_row").on("click", function () {
            // Dynamic Rows Code

            // Get max row id and set new id
            var newid = 0;
            $.each($("#tab_logic tr"), function () {
                if (parseInt($(this).data("id")) > newid) {
                    newid = parseInt($(this).data("id"));
                }
            });
            newid++;

            var tr = $("<tr></tr>", {
                id: "addr" + newid,
                "data-id": newid
            });

            // loop through each td and create new elements with name of newid
            $.each($("#tab_logic tbody tr:nth(0) td"), function () {
                var cur_td = $(this);

                var children = cur_td.children();

                // add new td and element if it has a nane
                if ($(this).data("name") != undefined) {
                    var td = $("<td></td>", {
                        "data-name": $(cur_td).data("name")
                    });

                    var c = $(cur_td).find($(children[0]).prop('tagName')).clone().val("");
                    c.attr("name", $(cur_td).data("name") + newid);
                    c.appendTo($(td));
                    td.appendTo($(tr));
                } else {
                    var td = $("<td></td>", {
                        'text': $('#tab_logic tr').length
                    }).appendTo($(tr));
                }
            });

            // add delete button and td
            /*
            $("<td></td>").append(
                $("<button class='btn btn-danger glyphicon glyphicon-remove row-remove'></button>")
                    .click(function() {
                        $(this).closest("tr").remove();
                    })
            ).appendTo($(tr));
            */

            // add the new row
            $(tr).appendTo($('#tab_logic'));

            $(tr).find("td button.row-remove").on("click", function () {
                $(this).closest("tr").remove();
            });
        });




        // Sortable Code
        var fixHelperModified = function (e, tr) {
            var $originals = tr.children();
            var $helper = tr.clone();

            $helper.children().each(function (index) {
                $(this).width($originals.eq(index).width())
            });

            return $helper;
        };

        $(".table-sortable tbody").sortable({
            helper: fixHelperModified
        }).disableSelection();

        $(".table-sortable thead").disableSelection();



        $("#add_row").trigger("click");
    });


</script>
