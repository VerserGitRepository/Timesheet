
@model IEnumerable<TimeSheet.Models.CostModelViewModel>

@{
    ViewBag.Title = "View";
}

<link href="~/Content/bootstrap.css" rel="stylesheet" />
<link href="https://cdn.datatables.net/1.10.19/css/jquery.dataTables.min.css" rel="stylesheet" />
<script src="https://code.jquery.com/jquery-3.3.1.js"></script>
<script src=" https://cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js"></script>
<script src="http://ajax.googleapis.com/ajax/libs/jqueryui/1.10.3/jquery-ui.min.js"></script>
<script src="https://cdn.datatables.net/1.10.15/js/dataTables.bootstrap4.min.js"></script>

<div class="container">
    <br />
    <div style="width:90%; margin:0 auto;">
        <table id="demoGrid" class="table table-striped table-bordered dt-responsive">
            <thead style="background-color:#FFA500">
                <tr>
                    <th>
                        @Html.DisplayName("OpportunityNumber")
                    </th>
                    <th>
                        @Html.DisplayName("Start Date")
                    </th>
                    <th>
                        @Html.DisplayName("End Date")
                    </th>
                    <th style="width:20px">
                        @Html.DisplayName("Site Address")
                    </th>
                    <th>
                        @Html.DisplayName("Customer")
                    </th>
                    <th>
                        @Html.DisplayName("Approver")
                    </th>
                    <th>
                        @Html.DisplayName("Project Manager")
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SalesManager)
                    </th>
                    <th>
                        @Html.DisplayName("Customer")
                    </th>
                    <th>
                        @Html.DisplayName("Branch")
                    </th>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.OpportunityNumber)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.StartDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.EndDate)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.SiteAddress)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Customer)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Approver)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.ProjectManager)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.SalesManager)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.CustomerContactName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.VerserBranch)
                        </td>

                    </tr>
                }
            </tbody>

        </table>
    </div>
</div>

<!-- Need to use datatables.net -->
<script src="https://cdn.datatables.net/1.10.16/js/jquery.dataTables.min.js"></script>
<script>

    $('#demoGrid').dataTable({
    });
</script>

@*@model  TimeSheet.Models.ienumarable<CostModelViewModel>
    @{
        ViewBag.Title = "Index";
    }
    <body>
        <br>
        <div class="container">
            <div class="row">
            </div>

            <div class="row">

                <div class="col-md-12">
                    <table id="datatable" class="table table-striped table-bordered" cellspacing="0" width="100%">
                        <thead style="background-color:#FFA500 ">
                            <tr>
                                <th>Opportunity</th>
                                <th>Project</th>
                                <th>Candidate</th>
                                <th>Activity</th>
                                <th>Date</th>
                                <th>Hours</th>
                                <th style="width:20px">Edit</th>
                                <th style="width:20px">Delete</th>
                            </tr>
                        </thead>

                        <tbody>
                            @foreach (TimeSheet.Models.CostModelViewModel x in Model)
                            {
                                <tr>

                                    <td>@x.OpportunityNumber</td>
                                    <td>@x.Customer</td>
                                    <td>@x.CustomerContactName</td>
                                    <td>@x.ServiceDescription</td>
                                    <td>@x.StartDate</td>
                                    <td>@x.EndDate</td>
                                    <td><p data-placement="top" data-toggle="tooltip" title="Edit"><button class="btn btn-primary btn-xs" data-title="Edit" data-toggle="modal" data-target="#edit"><span class="glyphicon glyphicon-pencil"></span></button></p></td>
                                    <td><p data-placement="top" data-toggle="tooltip" title="Delete"><button class="btn btn-danger btn-xs" data-title="Delete" data-toggle="modal" data-target="#delete"><span class="glyphicon glyphicon-trash"></span></button></p></td>


                                </tr>
                            }

                        </tbody>
                        </table>
                    </div>
                </div>
            </div>
     </body>*@

